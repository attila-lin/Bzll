enum CameraType {
    Game,
    SceneView,
    Preview,
    VR,
    Reflection
}

struct Camera {
    // Gets the temporary RenderTexture target for this Camera.
    activeTexture: RenderTexture,
    
}